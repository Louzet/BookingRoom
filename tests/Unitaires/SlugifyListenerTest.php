<?php
/*
namespace App\tests\Unitaires;

use App\Events\SlugifyListener;
use Doctrine\ORM\Event\LifecycleEventArgs;
use PHPUnit\Framework\TestCase;

class SlugifyListenerTest extends TestCase
{
    public function test_prepersite_event()
    {
        $prePersist = new SlugifyListener();

        $this->assertAttributeInstanceOf(LifecycleEventArgs::class, 'args', $prePersist->prePersist(LifecycleEventArgs::class));
    }
}*/
